{
  "id": "decode_lore",
  "title": "Decode Lore",
  "description": "Solve the sequence on mystic stones to reveal hidden lore.",
  "parameters": { "sequence": ["north","east","south"], "tileId": "mystic_stone" },
  "steps": [
    { "type": "input_sequence", "tile": "${tileId}", "sequence": "${sequence}" }
  ],
  "success": { "effect": { "type": "show_lore", "loreId": "grove_intro" } },
  "failure": { "effect": { "type": "show_lore", "loreId": "decode_hint" } }
}