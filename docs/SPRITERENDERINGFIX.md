# Sprite Rendering Fix

## Summary
- Implemented `draw()` on diagnostic entities to render hydrated sprites
- Ensured scene loop calls `entity.draw(ctx)` per frame

## Logs
- `[Draw] NPC sprite rendered at: x y`
- `[Draw] Spirit sprite rendered at: x y`